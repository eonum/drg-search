- if @codes.empty?
  .row
    .col-sm-12
      .has-warning.text-center= t('no_search_results')

- @codes.each do |code|
  %article.row
    %strong.col-sm-1= code.code
    .col-sm-11= ' ' + code.text(locale)